---
title: "Data Harmonization"
author: "Joe Corra"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
library(gt)
library(janitor)
```

## Data Harmonization

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r conventions}

tibble(standard = c("capitalization", "punctuation", 
                    "naming convention (case)", "numbers", "acronyms"),
       variable_names = c(), 
       value_names = c("sectors", "sources", "EIA MSN descriptions", 
                  "booleans (y/n or T/F)", "unit names", 
                  "US state and territory codes"), 
       metadata = c()) 
  
gt() %>% 
  gt::tab_header(title = "Key Data Standards for Harmonization") %>%
  opt_stylize(style = 6, color = "red")
```

